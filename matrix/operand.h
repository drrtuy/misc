#ifndef H_MATRIX_OPERAND
#define H_MATRIX_OPERAND

#include <string>

namespace matrix
{
    class Operand
    {
        public:
            Operand() { };
            Operand(std::string tokenStr) { };
            ~Operand() { };
    
    };

}

#endif

